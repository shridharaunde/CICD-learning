name: CI
 on:
 push:
 branches: [main]

 pull_request:
  branches: [main]
workflow dispatch:
 jobs:
  builds:
  runs-on: Ubuntu-latest
  steps:
   uses-action: actions/checkout@v3
   -name: run a one line script
   run: echo Hellow world!
   
   
  

  
 


















